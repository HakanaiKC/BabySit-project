@{
    ViewData["Title"] = "Đăng nhập";
}
@model BabySit.Models.User
<div class="d-flex align-items-center" style="min-height: 100vh">
    <div class="col-sm-8 col-md-6 col-lg-4 mx-auto" style="min-width: 300px;">
        <div class="card navbar-shadow">
            <div class="card-body">
                @using (Html.BeginForm("Login","Account",FormMethod.Post))
                {
                    <div class="form-group">
                        <h4 class="card-title text-center">Đăng nhập</h4>
                        @Html.LabelFor(model => model.Email)
                        <div class="input-group input-group-merge">
                            @Html.EditorFor(model => model.Email, new { htmlAttributes = new { @class = "form-control form-control-prepended" } })
                        </div>
                        @Html.ValidationMessageFor(mess => mess.Email, null, new { @class = "text-danger" })
                    </div>

                    <div class="form-group">
                        @Html.LabelFor(model => model.Password)
                        <div class="input-group input-group-merge">
                            @Html.EditorFor(model => model.Password, new { htmlAttributes = new { @class = "form-control" } })
                        </div>
                        @Html.ValidationMessageFor(mess => mess.Password, null, new { @class = "text-danger" })
                    </div>

                    <div class="form-group">
                        @Html.CheckBox("remember", false) Nhớ đăng nhập?
                    </div>

                    @if (ViewBag.Message != null)
                    {
                        <label class="text-danger">@ViewBag.Message</label>
                    }

                    <label style="color:green">@ViewBag.RegisterSuccessfully</label>
                    <label style="color:green">@ViewBag.MessageChangePasswordSuccess</label>
                    <div class="form-group ">
                        <input type="submit" value="Đăng nhập" class="btn btn-success btn-block" />
                    </div>
                }
                <div class="text-center">
                    <a asp-controller="Account" asp-action="ForgotPass" class="text-black-70" style="text-decoration: underline;">Quên mật khẩu?</a>
                </div>
                <div class="card-footer text-center text-black-50">
                    Chưa có tài khoản? <a asp-controller="Account" asp-action="Register">Đăng ký</a>
                </div>
            </div>
        </div>
    </div>
</div>
